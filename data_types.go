package main

import "fmt"

func print(){

	var favNum int = 7 // in go writing the variable starts with keyword var and then the variable name then its type and then its value
    var favName string = "confidential info"
	var negNum int16 = 1600
	var isgreat bool = true
	fmt.Println(favNum , favName , negNum , isgreat)

}